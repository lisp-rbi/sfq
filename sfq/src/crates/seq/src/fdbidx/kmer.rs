
use crate::{Fdb,Index};
use crate::util::error::Error;

impl Index for Fdb {

    fn make_kindex (&self) -> FxHashMap<String, String>  {
        // go through seq and chop the kmers

     }
     fn make_hdki (&self) -> FxHashMap<String, String>  {
         // HD index with encoded librustc_tsan//
         // first fill the index
         // then encode

      }
}
